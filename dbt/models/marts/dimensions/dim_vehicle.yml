version: 2
models:
  - name: dim_vehicle
    description: Vehicle dimension with capacities used for shipment validation
    columns:
      - name: vehicle_id
        tests:
          - not_null
          - unique
      - name: capacity_kg
        tests:
          - not_null
      - name: capacity_m3
        tests:
          - not_null
